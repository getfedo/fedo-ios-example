// Copy this file to Config/Secrets.xcconfig (gitignored) and set your key.
// Get your API key from the Fedo dashboard: https://getfedo.com
// Note: "//" starts a comment in xcconfig files, so the key must not contain it.

FEDO_API_KEY = your-fedo-api-key
